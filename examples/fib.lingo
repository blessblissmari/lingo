# recursion + a range loop. no allocations, no gc, no runtime.
# this should compile to roughly the same machine code as the equivalent C.

fn fib(n: u64) -> u64:
    if n < 2:
        return n
    return fib(n - 1) + fib(n - 2)

fn main():
    for i in 0..10:
        print(fib(i))
